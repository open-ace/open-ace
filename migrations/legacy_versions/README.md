Legacy Alembic revisions archived after introducing `baseline_2026_06_23`.

These files are kept for audit/reference and for legacy revision discovery
inside the baseline cutover helper, but Alembic no longer loads them from the
active `migrations/versions/` directory.
