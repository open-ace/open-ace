"""E2E governance tooling for Issue #2491.

File-level inventory, nodeid/entry-level debt state, promotion state, and the
mutually-exclusive lane selector that together form the E2E governance
baseline. All modules are pure-stdlib and identical locally and in CI
(the #2457 ``legacy_issue_baseline`` pattern).
"""
