"""Retired in #2429 batch 16.

The 7 sender_name parsing tests (TestResolveUserId) moved to
``tests/unit/test_sender_name_parsing.py`` with ``regression``/``issue(517)``
markers; bodies and assertions are unchanged there.

This directory keeps its server/browser-bound e2e files for the batch 17
exodus; this placeholder keeps the directory's file inventory intact and
collects zero tests. Remove it together with the rest of tests/issues/517
when that directory is drained.
"""
