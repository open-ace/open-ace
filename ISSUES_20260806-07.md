# Issue 记录：2026-08-06 ~ 2026-08-07 远程会话/AI 对话问题修复

> 本文件按用户要求过滤：**排除全部 403 相关 issue 及 2026-08-07 15 号及以后的 issue**（含 VS Code 相关条目）。保留 9 个 issue，拆分为 5 个 PR。

## PR 拆分建议

| PR | 包含 Issue | 主题 | 主改模块 |
|---|---|---|---|
| **PR-A** | #1 #2 #4 | 远程会话状态机与 token 生命周期 | 后端 |
| **PR-B** | #3 | 系统提示词泄露过滤（安全） | 后端 + 脚本 |
| **PR-C** | #5 | 远程项目可见 + 会话创建可诊断 | 后端 |
| **PR-D** | #11 #12 | webui 远程对话打通 | 前端 + 后端 |
| **PR-E** | #13 #14 | AI 消息可靠投递与 LLM 兼容 | agent + 后端 |

---

## PR-A：修复远程会话状态机与 token 生命周期（Issue #1 #2 #4）

**Labels**：`bug` `priority: critical` `backend` `session`
**Assignees**：`@open-ace-backend`

### #1 恢复会话后 SSE 一直转圈（Thinking）

**日期**：2026-08-06
**严重程度**：High

**现象**：恢复远程会话并发送"你好"后，webui 一直处于转圈等待状态，无任何回复（SSE 连接约 571 字节即 `[DONE]` 关闭）。

**根因**：gunicorn HUP reload 时 `_restore_in_memory_state` 把 DB 中 completed/error/stopped 会话全部写入 `_session_end_flags`（内存标记，只增不清）；会话恢复 active 后 `is_session_ended` 内存优先命中 → SSE 立即关闭。后端 LLM 实际工作正常（多次 200、DB 12 条输出），仅实时流被提前掐断。

**修复**：
- `remote_agent_manager.py`：新增 `clear_session_end_flag()`（pop 内存标记）
- `remote_session_manager.py`：会话置 active / resume 成功 / 新建会话共 4 处调用点清除标记

**验证**：docker cp + `kill -HUP 1` + `/readyz` 200，哈希一致。待浏览器复测。

**状态**：✅ 已修复

---

### #2 恢复会话发消息报 "[API Error: 401 Invalid or expired proxy token]"

**日期**：2026-08-06
**严重程度**：Critical

**现象**：Issue #1 修复后，恢复会话发送消息返回 401 token 失效错误。

**根因**（三层证据链）：
1. `_cmd_start_session` 执行后只回 session_status、**从不回 command_response ack** → 命令永远 `delivered`
2. `COMMAND_CLAIM_TIMEOUT_SECONDS=300`（5 分钟）→ 服务器每 5 分钟重新投递该命令（日志证实同一 start_session 每 5 分钟出现一次，间隔恰为 301s）
3. 旧 agent 代码把重复 start_session 的幂等情况报为 error → 服务器置会话 error → **连坐撤销该会话全部 proxy token** → 已启动的 qwen 进程持失效 token 无法自愈 → 每次 LLM 请求 401

**修复**：
- `agent.py` `_cmd_start_session`：结束前发送 command_response ack（根因修复，停止 5 分钟重投）
- `executor.py` `start_session`：带 `resume_session_id` 且已在运行 → 停旧进程、用新 token + `--resume` 重启
- `api_key_proxy.py`：新增 `has_valid_proxy_token()`
- `remote_session_manager.py` `resume_terminated_session`：幂等检查区分"有效 token 跳过" / "无 token 走恢复自愈"
- DB 清理：3 条 delivered start_session + 367 条其他 delivered 命令标记 responded

**验证**：新签发 token 直调 `/api/remote/llm-proxy/v1/chat/completions` 200；部署后 15 分钟无新 401/重投/命令堆积。

**遗留**：除 start_session 外其他命令（git_status 等）同样不 ack，会持续堆积 delivered 并 5 分钟重投（垃圾流量，不导致功能错误），建议后续统一补 ack（另开 P2 issue）。

**状态**：✅ 已修复

---

### #4 恢复远程会话报 INTERNAL SERVER ERROR（get_remote_session 500）

**日期**：2026-08-06
**严重程度**：High

**现象**：Issue #3 部署后，恢复远程会话报 `Failed to get remote session status: INTERNAL SERVER ERROR`；点"重新连接"又报 `Failed to create remote session. Check machine availability and access.`（连带表现，机器在线、会话 active）。

**根因**：Issue #3 的读取兜底过滤对 messages 元素直接调 `.get("role")`，但 `session_manager.get_messages` 返回的是 `SessionMessage` **dataclass**（有 `.role`/`.content`，无 `.get`）→ AttributeError → 500。

**修复**：`get_remote_session` 过滤逻辑兼容 dict 与 dataclass（`isinstance(m, dict) ? m.get : getattr`）。

**验证**：混合列表过滤无异常；`get_session_status` 直调返回正常；无新 Traceback/500。

**状态**：✅ 已修复

---

## PR-B：系统提示词泄露过滤（Issue #3）

**Labels**：`bug` `security` `backend` `privacy`
**Assignees**：`@open-ace-backend`

### #3 恢复历史对话后泄露系统提示词（显示为"用户"消息）

**日期**：2026-08-06
**严重程度**：Medium（安全/隐私）

**现象**：恢复远程会话后，历史对话中出现 `[Platform Tool Limits]`、`<system-reminder>` qwen 启动上下文、`Memory directory:` 指令、`Managed memory has TWO directories` 等内部提示词，以"用户（User）"身份显示。

**根因**（三层）：
1. qwen CLI 设计上把系统提示写成 `type=user`、`provenance=real_user` 消息存入 JSONL（必须以 user 角色进入 LLM 上下文）→ 无法仅凭 role 区分真实用户消息与系统上下文
2. open-ace 三条写入路径（agent 上报 `source=web_terminal`、LLM 代理 `source=llm_proxy`、历史抓取 `fetch_qwen.py`）均未过滤
3. webui 读取会话消息时按 role=user 渲染

**修复**（3 层方案）：
- 新建共享模块 `scripts/shared/qwen_context.py`：`is_qwen_system_context()` 特征识别
- 写入过滤：`remote.py agent_message`、`usage_sink.py _record_messages`、`fetch_qwen.py`
- 读取兜底：`get_remote_session` 防历史残留
- 数据清洗：session_messages（115 条）+ daily_messages（68 条）删除系统上下文记录（已备份可回滚）

**验证**：9 组用例通过（真实系统上下文→True，正常用户消息→False）；清洗后残留 = 0。

**状态**：✅ 已修复

---

## PR-C：远程项目可见性与会话创建可诊断（Issue #5）

**Labels**：`bug` `priority: high` `backend`
**Assignees**：`@open-ace-backend`

### #5 webui 项目选择器不显示远程项目 + 新建远程会话失败

**日期**：2026-08-06
**严重程度**：High

**现象**：新账号登录后项目选择器只有容器内项目 `/workspace/admin`，远程项目不显示；新建远程目录对话报 `Failed to create remote session. Check machine availability and access.`

**根因**：
1. `/api/projects` 只从 `projects` 表返回，不含 `agent_sessions` 里的远程会话
2. 新建会话失败是**非代码 bug**：agent 进程被 Ctrl+C 停止 → 机器 offline → `is_connected()` False；计划任务 `OpenACEAgent` 因 `python` 未用绝对路径启动失败（0x80070002）
3. `create_remote_session` 所有失败统一返回 None，路由只能回笼统文案，无法定位真实原因

**修复**：
- `projects.py` `_fetch_remote_projects`：从 `agent_sessions` 合并远程项目（含 `is_remote`）
- `session_history_sync.py`：新增 `encode_openace_path()`（webui SPA ko() 编码），远程会话 JSONL 按双编码写入
- `create_remote_session`：服务端自动签发 ha_pool_token（集成模式不带 token 时）；12 处失败点返回具体错误信息，路由透出真实原因
- 计划任务 action 改为 Python 绝对路径（需 UAC 提权重注册）

**验证**：`/api/projects` 返回 4 项目；`POST /api/remote/sessions` 端到端 200，会话创建成功。

**状态**：✅ 已修复

---

## PR-D：webui 远程对话打通（Issue #11 #12）

**Labels**：`bug` `priority: high` `frontend` `backend`
**Assignees**：`@open-ace-frontend`（#12 后端部分协办）

### #11 选择项目后无法对话 + 查看历史按钮跳项目选择列表

**日期**：2026-08-07
**严重程度**：High

**现象**（用户复测 4 点）：
1. 项目选择器选远程项目 → 会话区初始化正常但**和 AI 对话无效**（消息静默丢弃）
2. 左下角蓝云朵恢复对话正常（本地 JSONL 假象，非远程模式）
3. "查看历史会话"按钮点击后**先跳项目选择列表**，恢复后不能对话
4. 正确恢复的会话 UI 自动出现 AI 反馈（远程 agent offline + 积压命令重放）

**根因**：
- ChatPage 建立远程会话的 `useEffect` 依赖 `workspaceType/machineId` **全部来自 URL**；项目选择器导航 `S('/projects'+t)` 整页替换 URL 丢参数 → 判定本地模式 → 不 startSession → 消息被丢弃
- "查看历史"按钮用裸 `new URLSearchParams` 重建 URL 只留 `view=history` → 根路由渲染项目选择器
- 蓝云朵"正常"是本地同步 JSONL 假象
- 症状 4 = 远程 agent 心跳超时 offline + 积压命令重放
- 附带：`create_session` 的 `is_valid_path`（Linux 语义）拒绝远程 Windows 路径 → tracking 400
- 附带：start-agent.ps1 被意外清空、远程机器为旧版 7633B

**修复**：
- 重新启用 `patch-qwen-webui-navparams.py`：4 处导航保留 URL 参数（cache-bust `v=navparams-20260807c`）
- `workspace.py create_session`：`is_valid_path` 失败时若匹配 `^[A-Za-z]:[\\/]`（远程 Windows 路径）则放行
- Dockerfile 恢复 navparams RUN
- start-agent.ps1 恢复 8634B 最新版并同步到远程机器，重启 agent 恢复在线

**验证**：bundle 4 处 `window.location.search`；会话跟踪 400 消除；机器恢复在线。

**状态**：✅ 已修复

---

### #12 选择项目后仍不能对话（项目选择器数据无 machine_id）

**日期**：2026-08-07
**严重程度**：High

**现象**：Issue #11 后历史按钮/恢复正常，但**项目选择器选远程项目后仍不能对话**（AI 圈圈一闪而过）。

**根因**：`/api/projects` 远程项目未返回 `machine_id`；默认工作区 iframe URL 本就不带 workspaceType/machineId → E 点补丁只能保留"已有的"参数（本就不存在）→ ChatPage 本地模式 → 不 startSession。对照：恢复会话（open-ace 传入完整远程参数）正常。

**修复**（修改 3+4）：
- 后端：`projects.py` `_fetch_remote_projects` SQL 加 `remote_machine_id`，返回项加 `"machine_id"`
- 前端：项目选择器 E 点升级 v2——点击时查 machine_id，非空则 URL 追加 `workspaceType=remote&machineId=<id>`（cache-bust `v=navparams-20260807d`）

**验证**：bundle 含 `n.machine_id` 逻辑；SQL 3 个远程项目均带 machine_id；用户复测通过（选择项目→对话正常、403 消失）。

**状态**：✅ 已修复

---

## PR-E：AI 消息可靠投递与 LLM 兼容（Issue #13 #14）

**Labels**：`bug` `backend` `remote-agent`
**Assignees**：`@open-ace-agent`（agent 侧）+ `@open-ace-backend`（#14）

### #13 AI 重复收到同一条用户消息（send_message 缺 ack）

**日期**：2026-08-07
**严重程度**：Medium

**现象**：用户未说话仅点击"启动 VS Code"按钮，对话区 AI 自动回复"你好"且重复两次。

**根因**：agent `_cmd_send_message` **不回 command_response ack** → send_message 永久 `delivered` → 每 5 分钟重投（日志证实同一条消息出现 4 次，间隔恰为 301s；DB 有 3 条积压）。

**修复**：
- `agent.py` `_cmd_send_message`：加 command_response ack（成功/失败都回）
- 清理 DB 3 条 delivered send_message 积压

**状态**：✅ 已修复

---

### #14 DeepSeek 思考模式拒绝 tool_choice（AI 无回复）

**日期**：2026-08-07
**严重程度**：High

**现象**：发送需调用 shell 工具的指令（如"帮我创建一个名为txt的文件夹"）→ AI 无任何回复、图标消失；普通查询正常。

**根因**：qwen CLI 在强制调用工具时发送 `tool_choice` 字段，DeepSeek **思考模式不支持 tool_choice → 400**（`"Thinking mode does not support this tool_choice"`）→ LLM 调用失败 → 无回复。

**修复**：`llm_proxy_handler.py` 转发前移除非空 `tool_choice`（模型仍会按函数定义自动调用工具）。

**状态**：✅ 已修复

---

## 建议补充的后续跟踪 issue（不在本次范围）

- `[P2] agent _handle_command 统一补齐 command_response ack`（Issue #2 遗留，消除 delivered 命令堆积与 5 分钟重投）
- `[P3] 会话创建失败错误信息透出到 webui 前端`（#5 后端已透出，前端提示文案待优化）

---

## 说明

- 指派建议为占位符，请替换为实际 GitHub 用户名。
- 本文件已按用户要求过滤：不含 403 相关 issue、不含 Issue 15 及以后的条目（含 VS Code 相关）。
- 详细技术证据与备份清单见 `TROUBLESHOOTING_LOG.md`（问题 24 / 27 / 28 各轮次）。
